apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nginx
spec:
  selector:
    app: nginx
  ports:
  - port: {{ .Values.nginx.service.port }}
    targetPort: 80
  type: {{ .Values.nginx.service.type }}
