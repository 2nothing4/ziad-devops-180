apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  selector:
    app: api
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5000
  type: {{ .Values.service.type }}
